{
  "name": "report-software",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop application for a medical company: generate, store and share PDF reports.",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:api dev:desktop",
    "dev:api": "npm run dev --workspace @report/api",
    "dev:desktop": "npm run dev --workspace @report/desktop",
    "build": "npm run build --workspace @report/api && npm run build --workspace @report/desktop",
    "db:migrate": "npm run db:migrate --workspace @report/api",
    "db:seed": "npm run db:seed --workspace @report/api",
    "db:studio": "npm run db:studio --workspace @report/api",
    "typecheck": "npm run typecheck --workspace @report/api && npm run typecheck --workspace @report/desktop"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">=20"
  }
}

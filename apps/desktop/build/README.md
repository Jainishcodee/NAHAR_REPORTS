# Build resources

electron-builder reads packaging assets from this folder.

Drop a **256×256 (or larger) `icon.ico`** here and uncomment the `win.icon`
line in `../electron-builder.yml` to give the installer and app a custom icon.
A `installerIcon.ico` / `uninstallerIcon.ico` can be added for NSIS too.
